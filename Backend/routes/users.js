/*const express = require('express');
const userController = require('../controller/user.controller');
const router = express.Router();
const userController= require('../controller/usercontroller');

/* GET users listing. 
router.get('/', function(req, res, next) {
  res.send('respond with a resource');
});


router.get('/', userController.getAllAnimals);
router.get('/:id', userController.getAnimalById);
router.get('/:age', userController.getAnimalByAge);
router.get('/:height', userController.getAnimalByHeight);
router.post('/', userController.createAnimal);
router.put('/:id', userController.updateAnimal);
router.delete('/:id', userController.deleteAnimal);


module.exports = router;*/
